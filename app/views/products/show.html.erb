<section class="products-show">

  <header class="page-header">
    <h1><%= link_to @product.category.name, @product.category %> &raquo; <%= @product.name %></h1>
  </header>

  <article class="product-detail">

    <div class="row">

      <div class="col-sm-4">
        <%= image_tag @product.image.url, class: 'main-img' %>
      </div>

      <div class="col-sm-8">
        <dl class="dl-horizontal">
          <dt>Name</dt>
          <dd><%= @product.name %></dd>
          <dt>Description</dt>
          <dd><%= @product.description %></dd>
          <dt>Quantity</dt>
          <dd><%= @product.quantity %></dd>
          <dt>Price</dt>
          <dd><%= @product.price %></dd>
        </dl>
      </div>

      <div>
        <h3>Reviews</h3>

        <h4>Add a review why not?</h4>

        <%= form_for :review, url: "#{@product.id}/reviews" do |f| %>
          Name:<%= f.text_field :name %><br/>
          Product rating<%= f.select :rating, [0,1,2,3,4,5] %><br/>
          Description<%= f.text_field :description %>:

          <%= f.submit 'create' %>
        <% end %>

        <% if @reviews.size != 0 %>
          <% @reviews.each do |element| %>
            <%= render 'review', description: element.description, rating: element.rating %>
          <% end %>
        <% end %>

      </div>
    </div>
  </article>
</section>
